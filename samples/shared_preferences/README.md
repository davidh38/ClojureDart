# Run the `shared_preferences` sample:

1. `clj -M:cljd init`
2. `flutter pub add shared_preferences
3. `clj -M:cljd flutter` (considering you already have a simulator opened)
4. Press Save to Storage and close the app
5. Restart app to see data loaded from storage: `clj -M:cljd flutter`

